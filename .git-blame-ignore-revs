# Commits that changed formatting only, and should not stand between a line and
# its actual author.
#
# Honored automatically by GitHub and GitLab. For local `git blame`, it needs to
# be pointed at once per clone:
#
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# Only add a commit here if it is *purely* mechanical. A commit that reformats
# and also changes behaviour would hide the behaviour change, which is worse
# than the noise this file exists to remove.

# Reformat the crate with rustfmt (2026-07-29)
2a141f94dbd815dec02e6685ee9106c8ea6ec5a5
